# Copy to: ~/.config/opencode/opencode.env

# Optional: Context7 MCP (remote) API key (if you enable the MCP server in opencode.json)
CONTEXT7_API_KEY="put-your-key-here"

# Optional: MySQL MCP (if you enable it)
MYSQL_HOST="127.0.0.1"
MYSQL_PORT="3306"
MYSQL_USER="root"
MYSQL_PASSWORD="change-me"

# Optional: Filesystem MCP root (if you enable it)
OPENCODE_FS_ROOT="$HOME/Projects"

